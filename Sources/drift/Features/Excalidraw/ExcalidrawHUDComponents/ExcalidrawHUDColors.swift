import SwiftUI

extension Color {
    /// Accent color used for active Timer HUD ticks and text.
    static let excalidrawBg = Color(
        red: 175 / 255,
        green: 56 / 255,
        blue: 245 / 255
    )

    static let excalidrawAccent = Color(red: 0.49, green: 0.42, blue: 0.94)
}
